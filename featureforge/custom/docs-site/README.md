# docs-site

This directory is intentionally empty (aside from this file).

It's the **output** of FeatureForge's "Docs site" stream, registered at runtime rather than built in. Every file that appears here comes from describing a feature to `POST /api/features/docs-site/plan`, reviewing the diff, then `POST /api/features/docs-site/apply`.
